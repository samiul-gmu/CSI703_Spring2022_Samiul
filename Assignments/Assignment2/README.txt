I am attaching a .py file that is ready to be executed with Streamlit.

On top, I am also attaching a batch file with the necessary command to execute the .py file with Streamlit.

The batch file works given that the .py file and the batch file remain in the same folder on a windows machine.
